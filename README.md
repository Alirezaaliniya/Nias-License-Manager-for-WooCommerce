# 🔐 سیستم مدیریت لایسنس NLMW

سیستم مدیریت لایسنس برای افزونه‌های وردپرس با استفاده از License Manager for WooCommerce
https://wordpress.org/plugins/license-manager-for-woocommerce/
این افزونه رایگان باید در سایتی که از طریق آن افزونه میفروشید نصب باشد اسکریپت فعلی که در اینجا توسعه داده شده سازگار با این افزونه است و بصورت جدا عمل نمیکند

---

## 📥 نصب و راه‌اندازی

### مرحله 1: دانلود فایل‌ها

پوشه `includes` را در پوشه اصلی افزونه خود کپی کنید:

```
your-plugin/
├── includes/
│   └── license/
│       ├── class-license-manager-client.php
│       ├── class-license-settings-page.php
│       └── class-license-cron-handler.php
└── your-plugin.php
```

### مرحله 2: کپی کد مقداردهی اولیه

محتوای فایل `example-plugin.php` را به فایل اصلی افزونه خود کپی کنید.

### مرحله 3: تنظیمات

در فایل اصلی افزونه، سه بخش زیر را ویرایش کنید:

#### 1️⃣ تنظیمات افزونه

```php
$PLUGIN_CONFIG = array(
    'prefix'        => 'my-plugin',        // پیشوند یکتا افزونه
    'name'          => 'افزونه من',       // نام نمایشی
    'text_domain'   => 'my-plugin',        // دامنه متنی
);
```

#### 2️⃣ تنظیمات فروشگاه

```php
$STORE_CONFIG = array(
    'url'              => 'https://mystore.ir',  // آدرس فروشگاه
    'consumer_key'     => 'ck_xxxxx',            // کلید API
    'consumer_secret'  => 'cs_xxxxx',            // رمز API
);
```

**نحوه دریافت کلیدهای API:**
1. وارد پنل مدیریت ووکامرس شوید
2. بروید به: ووکامرس > تنظیمات > پیشرفته > REST API
3. روی "افزودن کلید" کلیک کنید
4. دسترسی "خواندن" را انتخاب کنید
5. کلیدها را کپی کنید

#### 3️⃣ تنظیمات لایسنس

```php
$LICENSE_CONFIG = array(
    'product_ids'  => array( 123, 456 ),  // شناسه محصولات مجاز
    'cache_days'   => 5,                   // مدت کش (روز)
);
```

**نحوه پیدا کردن Product ID:**
1. وارد پنل مدیریت ووکامرس شوید
2. بروید به: محصولات > همه محصولات
3. روی محصول کلیک کنید
4. در URL صفحه، عدد بعد از `post=` همان Product ID است

مثال: `post.php?post=123` → Product ID = 123

---

## ✅ تست عملکرد

بعد از نصب:

1. افزونه را فعال کنید
2. بروید به: تنظیمات > لایسنس [نام افزونه]
3. یک کلید لایسنس وارد کنید
4. روی "فعال‌سازی لایسنس" کلیک کنید

---

## 🎯 ویژگی‌ها

✅ بررسی شناسه محصول (Product ID)  
✅ بررسی موجودی فعالسازی  
✅ بررسی تاریخ انقضا  
✅ بازگشت موجودی در غیرفعالسازی  
✅ بررسی خودکار دوره‌ای  
✅ رابط کاربری فارسی  
✅ پیام‌های خطای واضح  

---

## 📝 استفاده در کد

برای بررسی وضعیت لایسنس در کد افزونه:


```php
if ( license_valid() ) {
    // لایسنس معتبر - نمایش ویژگی پریمیوم
} else {
    // لایسنس نامعتبر - نمایش محدودیت
}

```

---

## 🔧 تنظیمات پیشرفته

### قبول همه محصولات

اگر می‌خواهید همه محصولات را بپذیرید:

```php
'product_ids'  => array(),  // خالی = همه محصولات
```

### تغییر فاصله بررسی خودکار

```php
'cache_days'   => 7,  // هر 7 روز یکبار
```

---

## 🐛 عیب‌یابی

### لایسنس فعال نمی‌شود

1. کلیدهای API را بررسی کنید
2. Product ID را بررسی کنید
3. موجودی فعالسازی را بررسی کنید

### خطای "شناسه محصول مطابقت ندارد"

Product ID لایسنس با لیست مجاز مطابقت ندارد. Product ID صحیح را در تنظیمات وارد کنید.

---

## 📞 پشتیبانی

برای گزارش مشکل یا سوال، یک Issue در GitHub ایجاد کنید.
